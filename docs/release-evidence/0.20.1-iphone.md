# 0.20.1 physical-iPhone and Sync evidence record

- Candidate version: 0.20.1
- Candidate commit: The merged commit carrying release tag `0.20.1`
- Date: 2026-09-10
- Status: **Maintainer-authorized waiver — physical-iPhone matrix not executed; controlled two-device Sync unexecuted**
- Physical-device tester: None for the final sticky-toolbar candidate
- Physical iPhone / iOS / Obsidian Mobile / Sync provider: Unverified for this candidate
- Tester signature or initials: Unsigned — no final-candidate physical-device sign-off

## Candidate-specific authorization

After the tested sticky-toolbar candidate and its remaining physical-iPhone verification gap were explicitly reported, the maintainer directed **“publish”**. This records authorization for 0.20.1 only with that scope unverified. It is not a physical-device Pass, does not convert an unexecuted check into evidence, and must not be reused for another candidate. The separate 0.20.0 and earlier authorizations do not carry forward.

A user-provided physical-iPhone screenshot showed an earlier compact, nonsticky mobile build. It establishes only the visible state of that earlier build, not the behavior of the final sticky toolbar, its Filters panel, touch-event handling, or the complete device matrix. That screenshot and its private titles are not reproduced in this record or release assets.

## Unverified scope

The final candidate's physical-iPhone matrix not executed includes portrait/landscape touch behavior, software-keyboard and visual-viewport changes, safe areas and Obsidian's bottom toolbar, largest Dynamic Type, VoiceOver, Arabic/RTL, background/interruption, device performance, native file-picker cancellation, import/export and recovery. Controlled two-device Sync handoff, delayed delivery, offline concurrent edits and reconciliation also remain unexecuted. Automated merge tests and desktop emulation cannot establish provider or device-pair behavior.

The [candidate smoke record](next-release-smoke.md) retains each physical-device and Sync row as **Unexecuted**. The [manual physical-iPhone checklist](../manual-iphone-release-checklist.md) remains the procedure for executing them. The waiver accepts release risk; it is neither evidence that the untested behavior works nor proof that it is defective.

## Supplemental implementation validation

Before assigning 0.20.1 release metadata, the completed mobile implementation was checked on 2026-09-10:

- `npm run review` passed 1,187/1,187 runtime tests, enforced coverage floors, all three performance budgets and all ten release tests, together with typecheck, zero-warning lint/JSON validation, the production build, bundle budgets and Community static checks.
- `npm run test:layout` passed 117/117 browser cases: 66 Chromium and 51 WebKit, with zero failures, skips or flakes. The production-renderer cases checked reduced phone leaves at 320, 390 and 402 CSS-pixel widths, enlarged text, a short resized viewport, sticky control hit-testing, independently scrolling Filters, Escape, refresh/return focus and scroll, reverse keyboard navigation, and exact-note and Clear activation through browser-generated mouse and touch events.
- Real Obsidian 1.13.7 on macOS, in a disposable synthetic vault, passed four viewport checks and four interaction checks. The phone-sized runs used Obsidian's desktop mobile emulation. The compact pinned toolbar measured 106 CSS pixels; tabs, Search and Filters remained reachable while the larger header and counts scrolled away. The last note cleared the native bottom toolbar, and the measured desktop layout remained identical to the baseline.
- The native interactions verified Filters opening at a deep scroll without moving the list, filter values and closed state surviving refresh, exact-note selection and Back preserving the query and visible focus, and the visible Clear action. Synthetic Markdown note bytes remained unchanged, with no captured renderer errors.

These are supplemental desktop-runtime and browser results, not physical iOS, physical Dynamic Type, VoiceOver or two-device Sync passes. Browser fixtures use the actual KBCC renderer and stylesheet with a narrow synthetic Obsidian host, not a live vault. The software-keyboard viewport is modeled; browser touch events are not a physical-iPhone certification.

## Release gate and privacy boundary

The versioned 0.20.1 local gate on 2026-09-10 used Node 25.8.1 on macOS and passed 1,188/1,188 runtime tests, all three performance budgets, all ten release tests, coverage floors, typecheck, zero-warning lint/JSON validation, build, bundle and Community checks. The complete browser suite passed 117/117 cases (66 Chromium, 51 WebKit), with zero failures, skips, flakes or retries. The four updated What's New modal cases and their synthetic mobile/desktop screenshots were checked for version, text, link, wrapping and reachable actions. The dependency audit reported zero vulnerabilities. The archive contained only `main.js`, `manifest.json` and `styles.css` and passed its privacy check.

The JavaScript measured 1,136,691 raw / 297,341 gzip bytes; CSS measured 149,586 raw / 20,465 gzip bytes. Existing limits were not raised. These local results do not claim that the publication workflow has already succeeded. The exact tagged build must independently pass the Node 22 release workflow, including tag ancestry, dependency audit, automated checks, checksums and artifact attestations. The immutable release URL is `https://github.com/drbinsaad/knowledge-base-command-center/releases/tag/0.20.1`; its availability and published assets must be verified after publication.

No screenshots, private notes, organization data, recovery exports, local absolute paths or credentials are included in this evidence record. Any later physical-device run may append actual observations, but must preserve that the final sticky candidate's physical scope was unverified when publication was authorized.
