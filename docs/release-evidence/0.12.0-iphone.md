# 0.12.0 physical-iPhone evidence record

- Candidate version and commit: 0.12.0 at `02c1430` (plus this evidence commit)
- Date: 2026-08-12
- Status: **Waived — not executed.**

The maintainer explicitly waived the physical-iPhone release gate for 0.12.0 with
the instruction "publish with item 41 unverified". Item 41 is the post-update
**What's new** window check, added after the maintainer's earlier device pass on
the pre-announcement candidate. No device run took place for this exact head, so
no device metadata is recorded here; nothing in this file is invented.

Automated coverage that did run for this candidate: 670 runtime tests and
10 release tests (including synthetic-DOM update-announcement, keyboard-geometry,
stacked-pane, and 44-point checks), TypeScript, ESLint, JSON validation, the
production build, Community static verification, and dependency audits with zero
findings, plus an independent review with reproductions of the store version 14
Sync behavior.

Outstanding device checks inherited by the next mobile-affecting release: the
complete `docs/manual-iphone-release-checklist.md` matrix, including item 41
(update window: once-only display after an actual upgrade, no reopening after
two reloads, manual **Open what's new** reopening exactly one window,
portrait/landscape/Largest Text/VoiceOver behavior, and browser return from the
release link).
