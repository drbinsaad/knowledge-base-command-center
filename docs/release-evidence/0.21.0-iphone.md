# 0.21.0 physical-device and Sync evidence record

- Candidate version: 0.21.0
- Candidate commit: The release commit intended for tag `0.21.0`; this record is prepared before tagging
- Date: 2026-09-12
- Status: **Maintainer-authorized waiver — physical-iPhone matrix not executed; physical iPad matrix and controlled two-device Sync unexecuted**
- Physical-device tester: None for this candidate
- Physical iPhone / iPad / iOS / iPadOS / Obsidian Mobile / Sync provider: Unverified
- Tester signature or initials: Unsigned — no physical-device sign-off

## Candidate-specific authorization

After the completed iPad-layout and single-note organization candidate, its automated/native-emulation results, and its remaining physical-device verification gap were explicitly reported, the maintainer directed **“Publish”**. This is publication authorization for 0.21.0 with the stated scope unverified. It is not a physical-device Pass and does not convert any unexecuted check into evidence. The separate 0.20.1 and earlier waivers are not reused and do not authorize this candidate.

Earlier screenshots and release records are historical observations only. They do not establish that this candidate works on a physical device and are not reproduced in this record.

## Unverified scope

The final candidate has not been tested on a physical iPhone or iPad. This includes portrait/landscape touch, iPad landscape and Split View, native iOS/iPadOS select behavior, software-keyboard and visual-viewport changes, safe areas and Obsidian chrome, a physical hardware keyboard, largest Dynamic Type, VoiceOver, Arabic/RTL, background/interruption, device performance, native file-picker cancellation, import/export and recovery.

Controlled two-device Sync handoff, delayed delivery, offline concurrent organization edits, and reconciliation are also unexecuted. Automated merge/stale-state tests do not establish a Sync provider's behavior on a device pair. Desktop browser keyboard events do not verify a physical mobile keyboard or assistive technology.

The [0.21.0 smoke record](next-release-smoke.md) leaves every physical-device and Sync row **Unexecuted**. The [manual checklist](../manual-iphone-release-checklist.md) provides the procedure and the candidate-specific additions. The waiver accepts release risk; it is neither evidence that untested behavior works nor proof that it is defective.

## Supplemental implementation validation

The completed implementation was checked on 2026-09-12, before assigning the 0.21.0 release metadata:

- The full local implementation review passed 1,226/1,226 runtime tests, enforced coverage floors, all three performance budgets, and all ten release tests, together with typecheck, zero-warning lint/JSON validation, the production build, applicable bundle budgets, and Community static checks.
- The complete Chromium/WebKit browser suite passed 141/141 cases. Its production-renderer cases exercised wide mobile single-column routing, sticky controls, search and inspector Back, the short single-note flow, explicit Index parent/root choices, searchable bounded parent options, review/no-change behavior, base selection, and keyboard/focus behavior. These use synthetic browser hosts, not Obsidian Mobile or a physical device.
- Real Obsidian 1.13.7 on macOS completed ten supplemental checks in a disposable synthetic vault using Obsidian's own mobile emulation. At 1180 and 1366 CSS-pixel widths, the list occupied the full available workspace width and the 106-pixel toolbar stayed pinned after scrolling. At 1180 and 402 pixels, native Obsidian select controls measured 44 pixels high and hidden simple-mode controls occupied no space.
- In that real Obsidian runtime, the visible note badge opened **Choose location → Review** directly. An existing synthetic placeholder parent was chosen, the exact before/after placement was reviewed, and **Save organization** was activated through the UI. Session Undo and Redo restored the exact complete personal and portable organization snapshots. Reload retained the same parent, portable identity, Index membership and record kind, and the short flow prefilled the saved location.
- The independently read serialized plugin store matched the reviewed result after the model's normal optional-field canonicalization. The reload omitted an optional `libraryId: null` on an indexed subject; no other organization difference remained. Exact raw Undo/Redo comparisons were retained. All 26 synthetic Markdown files kept their SHA-256 hashes, and no renderer or error-level console errors were captured. Closing the inspection-only draft changed no organization.

These are implementation-level browser and desktop-runtime results only. They are not physical iPhone/iPad, iOS WebKit, Dynamic Type, VoiceOver, hardware-keyboard, or two-device Sync passes. No runtime test shim was substituted for Obsidian during the native UI actions.

## Versioned release gate

The fresh versioned 0.21.0 local gate completed on 2026-09-12 under Node 25.8.1 on macOS:

- `npm run review` exited successfully with 1,229/1,229 runtime tests, zero failures and zero skips. Coverage was 87.72% lines, 81.64% branches and 82.30% functions, above the enforced floors.
- All three performance budgets and all ten release checks passed, together with strict typecheck, zero-warning lint, six JSON configuration validations, the production build, bundle budgets and Community static checks. Release checks covered the current announcement, version and exact immutable release link.
- The complete browser suite passed 141/141 cases: 78 Chromium and 63 WebKit, with zero failures, skips, flakes or reported errors.
- The dependency audit reported zero vulnerabilities at every severity.
- JavaScript measured 1,152,724 raw / 301,882 gzip bytes; CSS measured 149,864 raw / 20,511 gzip bytes. These fit the declared feature budgets of 1,160,000 / 305,000 and 150,000 / 20,600 bytes respectively.
- The packaged ZIP contained exactly `main.js`, `manifest.json` and `styles.css` and passed the privacy check. Its SHA-256 was `895a9d7b0540cb20a539d3bbcee7e4fbef9857741aead9badcebd5132dcc4fd2`.

These are completed local checks, not a claim that CI or publication has succeeded. The exact tagged build must independently pass the Node 22 publication workflow, including tag ancestry, dependency audit, checksums and artifact attestations. Node 22 CI results and publication availability were not yet verified when this record was finalized. Neither local automation nor future CI executes the physical-device or controlled Sync matrix.

The intended immutable release URL is `https://github.com/drbinsaad/knowledge-base-command-center/releases/tag/0.21.0`. Availability and published assets must be verified after publication; the link itself is not evidence that a release already exists.

## Privacy boundary

This public record contains no vault path, note title or body, plugin organization export, recovery file, screenshot, device identifier, local absolute path, credential, or patient information. Detailed synthetic runtime receipts remain outside the repository. A later physical-device run may append actual observations, but must preserve that publication was authorized while this candidate's physical-device and controlled Sync scope was unverified.
