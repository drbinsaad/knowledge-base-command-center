# 0.23.1 physical-device and Sync evidence record

- Candidate version: 0.23.1, the release commit intended for tag `0.23.1`
- Date: 2026-09-16
- Status: **Maintainer-authorized waiver — physical iPhone/iPad and controlled two-device Sync unexecuted**
- Physical-device tester: None; physical-iPhone matrix not executed; no physical-device sign-off
- Physical device / iOS / iPadOS / Obsidian Mobile / Sync provider: Unverified

## Candidate-specific authorization

After the cover-setup and mobile-settings fixes were tested and physical iPhone/iPad validation was explicitly disclosed as unverified, the maintainer answered **“Yes, publish the tested update”** to the publication question. This is fresh authorization for 0.23.1 only, not a physical-device Pass, and must not be reused for another candidate. Earlier release waivers do not provide current device sign-off.

The scope is local cover resolution, safe diagnostic labels, and explicit Library display saving in a fixed-visible footer. Online images remain unsupported. No note content, data schema, dependencies, permissions or network behavior are changed. The update does not establish the exact cause of the originally reported cover failure on the user's device.

## Unverified scope

Physical iPhone/iPad portrait, landscape and Split View; finger/Pencil/trackpad interaction; native scrolling; real software/hardware keyboard changes; safe areas; largest Dynamic Type; Arabic/RTL on devices; VoiceOver; background/interruption; image memory and device performance; and controlled two-device Sync remain unexecuted. Chromium/WebKit tests in a synthetic Obsidian host do not execute these rows. No fresh native Obsidian pass is claimed.

The [active smoke record](next-release-smoke.md) and [manual checklist](../manual-iphone-release-checklist.md) retain this scope as **Unexecuted**. Only future observations of the exact build on actual devices can change an individual row's status.

## Local implementation validation

Before assigning the release version, Node 22.23.2 passed 1,394 runtime tests, three performance budgets and ten release checks, plus typecheck, zero-warning lint/JSON, production build, bundle budgets and Community-oriented static checks. Coverage was 87.73% lines, 82.34% branches and 83.83% functions. All 269 Chromium/WebKit browser cases passed; the focused 60-case gallery/privacy matrix also passed separately.

The browser checks cover phone, tablet and desktop sizes, light/dark and LTR/RTL fixtures, compact placeholders, cover-property capitalization and local Markdown links, and settings drafts, Save visibility/hit-testing before scrolling, saved-state feedback and reopen persistence. Short visual-viewport fixtures include a 320px-high keyboard-like viewport with a vertical offset. Synthetic phone, tablet, keyboard-like and dark-desktop screenshots were inspected. These are not native Obsidian or physical-device results.

## Final versioned local validation

The versioned `npm run review` gate passed on Node 22.23.2: **1,395 runtime tests, three performance budgets and ten release tests**, with typecheck, zero-warning lint/JSON, production build, bundle limits and Community-oriented static verification. Enforced coverage was 87.75% lines, 82.43% branches and 83.83% functions. `npm run audit:high` reported zero vulnerabilities. JavaScript limits remain 1,205,000 raw / 316,000 gzip bytes. The bounded CSS limits are 160,500 raw / 22,500 gzip bytes, accounting for 1,930 raw / 256 gzip added footer bytes over 0.23.0.

- `main.js`: 1,202,660 raw / 315,907 gzip bytes; SHA-256 `c0c2f25d6cf553b5863f73c9de31b2cf85b8a0627d9df091b1ab2f2cae70e886`
- `manifest.json`: SHA-256 `5117bc7886339ed1846425e5b4f290554616e16295bfa959a721f5a7bd0d4320`
- `styles.css`: 159,838 raw / 22,069 gzip bytes; SHA-256 `63b09d6a6f62f846743be5e3507521cf016dcdde0faef3524500139a9c1da3ad`
- The local archive contains exactly `main.js`, `manifest.json` and `styles.css`; its privacy check passed. ZIP SHA-256 `f29b9a38e53f69d5d931376690a28f18b6297fdc1be10bfa2a12675741709a78`.

The versioned full `npm run test:layout` matrix passed all **269 Chromium/WebKit cases in 2.2 minutes**, with no failures or retries. The gallery implementation was unchanged during this run; the final shorter update-announcement copy was separately covered by the final runtime/release checks. Required GitHub CI repeats the combined gate on the committed tree. These remain synthetic-host browser results, not native or physical-device sign-off.

## Publication boundary

Node 22 required CI and the tag workflow must pass before publication. The release workflow verifies main-branch ancestry, matching version metadata, dependency audit, full runtime and browser gates, checksums and build provenance. The intended release URL is `https://github.com/drbinsaad/knowledge-base-command-center/releases/tag/0.23.1`. Asset availability, downloaded hashes, provenance and the update-facing manifest are verified separately after publication; a local check does not establish remote publication.

## Format, rollback and privacy

Formats remain those introduced in 0.23.0: store/base schema 16, portable 6, Workspace 3 and recovery 12. This patch adds no format migration. Public 0.22.0 and older builds cannot edit newer organization safely; an older-schema downgrade requires matching older plugin assets and a compatible pre-upgrade plugin-data snapshot together. Use synthetic disposable vaults for migration, destructive and controlled Sync checks.

No real notes, plugin-data exports, absolute local paths, private screenshots, device identifiers, credentials or patient information are included in this public record. Detailed synthetic receipts remain outside the repository. This release does not install into or modify the maintainer's main vault. This is an explicit device-scope waiver, not approval of the deferred online-cover proposal.
