# 0.20.0 physical-iPhone and Sync evidence record

- Candidate version: 0.20.0
- Candidate commit: The merged commit carrying release tag `0.20.0`
- Date: 2026-09-09
- Status: **Maintainer-authorized waiver — physical-iPhone matrix not executed; controlled two-device Sync unexecuted**
- Physical-device tester: None; no physical iPhone was connected
- Physical iPhone / iOS / Obsidian Mobile / Sync provider: Unverified

## Candidate-specific authorization

After the tested local candidate and the remaining physical-iPhone, VoiceOver, and controlled two-device Sync gaps were explicitly reported, the maintainer directed **“publish”**. This accepts those unverified scopes for 0.20.0 only. It is not a physical-device Pass, does not turn a skipped item into evidence, and must not be reused for a later candidate. Historical release waivers remain unchanged.

## Unverified scope

The physical-iPhone matrix not executed includes portrait/landscape touch behavior, iOS WebKit, software keyboard, safe areas, largest Dynamic Type, VoiceOver, Arabic/RTL, background/interruption, large-vault device latency, native file-picker cancellation and recovery behavior. Controlled two-device Sync handoff, offline concurrent edits and reconciliation are also unexecuted. Automated merge tests do not establish provider or device-pair integration behavior.

The [candidate smoke matrix](next-release-smoke.md) retains every physical-device and Sync row as **Unexecuted**. The [full manual checklist](../manual-iphone-release-checklist.md) remains the procedure for future testing.

## Supplemental implementation validation

Before release metadata was assigned, the same implementation passed on macOS with Node 25.8.1 and Playwright 1.62.1:

- `npm run review`: 1,175 runtime tests, enforced coverage floors, three performance budgets, typecheck, zero-warning lint and JSON validation, production build/bundle budgets, Community static checks and ten release tests.
- Chromium/WebKit: 83 browser cases passed, zero failures, skips or flakes. Ten added Settings geometry cases cover 320–1440px widths, long base names, keyboard focus and compact 44px controls.
- Real Obsidian 1.14.1, synthetic disposable vault: Settings labels and management dialogs, Unlink cancellation, search correctness, generation invalidation and cancellation. All six synthetic Markdown file bodies remained unchanged.
- Native Settings geometry and keyboard management passed at 320, 390, 900 and 1200px with no captured page or console errors. The compact control area changed from 320px to 44px at 900px and to 96px at phone widths; the wide layout remained unchanged.
- The test runtime was desktop Obsidian/Electron, including its mobile emulation, not physical iOS. Browser fixtures use a narrow synthetic Obsidian host and are not captures of a real vault or evidence of iPhone/VoiceOver/Sync testing.

The 250,000-note / 50-base search benchmark used the production search implementation in a synthetic Chromium host. Cold queries measured 2.33–2.61 seconds and repeat queries 1.20–1.32 seconds with unchanged counts/results; it excludes real vault I/O and physical-device behavior.

No private notes, plugin organization files, local absolute paths, credentials or personal screenshots are included in the release artifacts.

## Final versioned local gate

On 2026-09-09, the 0.20.0 metadata and curated update announcement were included in a fresh local run on Node 25.8.1:

- `npm run review` passed 1,176/1,176 runtime tests, coverage of 87.23% lines / 80.84% branches / 81.58% functions, all three performance tests and all ten release tests, plus typecheck, lint/JSON, production build, bundle limits and Community checks.
- `npm run test:layout` passed 87/87 cases (51 Chromium, 36 WebKit), with zero failures, skips or flakes. The new announcement was verified on desktop and phone-sized browser viewports, including readable highlights, reachable controls, keyboard dismissal and its exact immutable release URL.
- `npm run audit:high` reported zero vulnerabilities.
- `node scripts/package-release.mjs` produced an archive containing exactly `main.js`, `manifest.json` and `styles.css`; the privacy check passed. `npm run release:checksums` generated checksums for those same three assets.
- The production bundle measured 1,130,099 raw / 295,536 gzip bytes; the stylesheet measured 145,853 raw / 19,896 gzip bytes. Both are within the enforced budgets.

The first local browser launch was denied by the macOS execution sandbox before app tests could run. The complete rerun with the required process permission produced the passing counts above; this was an environment failure, not a discarded product regression.

## Release evidence boundary

The exact versioned build is checked by the release workflow on Node 22 before publication. Its tag ancestry, automated checks, checksums and provenance attestations are independent automated evidence, not physical-device validation. The immutable release page is `https://github.com/drbinsaad/knowledge-base-command-center/releases/tag/0.20.0`.

A later device run may append real observations, but must preserve that these scopes were unverified when 0.20.0 was authorized.
